meta {
  name: Create a document in a collection
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/data/:databaseId/:collectionId
  body: json
  auth: bearer
}

params:path {
  databaseId: 
  collectionId: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
